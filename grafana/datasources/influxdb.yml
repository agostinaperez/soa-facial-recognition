apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086  # Nombre del servicio
    database: metrics_db
    isDefault: true
    jsonData:
      httpMode: GET            # Requerido para InfluxQL clásico