meta {
  name: Cargar imagenes
  type: http
  seq: 1
}

post {
  url: {{base_url}}/persons/{{current_personId}}/embeddings
  body: json
  auth: inherit
}

body:json {
  {
    "images": {{{encoded_images}}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
